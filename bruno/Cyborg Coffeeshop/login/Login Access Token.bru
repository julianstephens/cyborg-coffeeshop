meta {
  name: Login Access Token
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/login/access-token
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  username: admin@example.com
  password: iPlJZ72cxl00
  scope: user:me
  ~grant_type:
  ~client_id:
  ~client_secret:
}

script:post-response {
  bru.setEnvVar("token", res.getBody().access_token)
  bru.setEnvVar("test", res.body.access_token)
}
